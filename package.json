{
  "name": "hardhat-project",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.0",
    "@nomiclabs/hardhat-ethers": "npm:hardhat-deploy-ethers@^0.3.0-beta.13",
    "dotenv": "^16.0.3",
    "ethers": "^5.7.2",
    "hardhat": "^2.12.2",
    "hardhat-deploy": "^0.11.20"
  },
  "scripts": {
    "compile": "hardhat compile",
    "deploy:aurora": "hardhat --network aurora-testnet deploy",
    "deploy:aurora:reset": "hardhat --network aurora-testnet deploy --reset"
  }
}